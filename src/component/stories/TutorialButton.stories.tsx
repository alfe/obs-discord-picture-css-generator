import { Meta, StoryObj } from '@storybook/react'
import TutorialButton from '../TutorialButton';

export default { component: TutorialButton } as Meta;

export const Default: StoryObj = {
  args: {},
};
